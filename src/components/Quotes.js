import quoteStyle from '../style/quote.module.css';

const Quotes = () => (
  <div>
    <ul className={quoteStyle.list}>
      <li>“Technology is best when it brings people together.”</li>
      <li>“Keep going forward because success will come”</li>
      <li>“Engineering is the closest thing to magic that exists in the world.”</li>
    </ul>
  </div>
);

export default Quotes;
